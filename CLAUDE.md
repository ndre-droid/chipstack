# ChipStack — project delta

Poker chip-distribution app. Stack: Vite + React + TypeScript, wrapped for Android via
**Capacitor** (not Flutter — differs from my global default).

- APK packaging: no local JDK/Android SDK — can't build the APK locally yet.
- Keystore lives at `keystore` — never read or print its contents (also globally denied).
- `HANDOFF.md` has prior context — check it before assuming project state.
